info:
  name: Listar Productos by ID
  type: http
  seq: 2

http:
  method: GET
  url: "{{producto-url}}/api/v1/productos/1"
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
