info:
  name: Descontar Stock
  type: http
  seq: 6

http:
  method: PUT
  url: "{{producto-url}}/api/v1/productos/1/descontar-stock"
  body:
    type: json
    data: |-
      {
        "cantidad": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
