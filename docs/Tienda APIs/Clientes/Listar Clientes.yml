info:
  name: Listar Clientes
  type: http
  seq: 1

http:
  method: GET
  url: "{{cliente-url}}/api/v1/clientes"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
