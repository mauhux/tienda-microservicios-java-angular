info:
  name: Listar Cliente by ID
  type: http
  seq: 2

http:
  method: GET
  url: "{{cliente-url}}/api/v1/clientes/1"
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
